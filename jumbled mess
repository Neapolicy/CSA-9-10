import java.util.Random;
public class Coin
{
    private double mass;
    private String side = "Heads";

    public void rigged_flip()
    {
        System.out.println("It's heads");
    }
    public void normal_flip(String side)
    {
        Random rand = new Random();
        int random_int = rand.nextInt(1);
        if (random_int == 0)
        {
            
        }
    }
}
